import { Global } from "@emotion/react";

const Fonts = () => (
  <Global
    styles={`
      
      @font-face {
        font-family: 'Nunito Sans', sans-serif;       
        
      }
      `}
  />
);

export default Fonts;
